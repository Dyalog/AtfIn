 state←∆IDLOC objlist
⍝ Return a numeric matrix of class codes with one row for each object
⍝ in objlist and one column for each stack level (most local on left).

 state←⍪(≢objlist)⍴¯1
 →0
 state←⎕STATE¨↓⍣(1≡≡objlist)⌷objlist
 ∘∘∘

⍝ more...
